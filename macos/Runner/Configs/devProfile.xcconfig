#include "../../Flutter/devProfile.xcconfig"
#include "Warnings.xcconfig"

ASSET_PREFIX=dev
BUNDLE_NAME=Beacon (Dev)
BUNDLE_DISPLAY_NAME=Beacon (Dev)
FLUTTER_TARGET=lib/main_dev.dart
CODE_SIGN_ENTITLEMENTS=Runner/Release.entitlements
